# simple-generating-text-with-rnn

A simple RNN character generator. Given previous characteres as input, then predict next character.

Before you train, you need to download dataset.
```
wget http://www.gutenberg.org/files/11/11-0.txt
```
